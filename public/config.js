// Runtime config — sobrescrito pelo entrypoint.sh via envsubst
// Configure via variáveis de ambiente no EasyPanel, não edite aqui.
window.__BARBER_CONFIG__ = {
  EVO_URL:      '',
  EVO_INSTANCE: 'barberflow',
  EVO_APIKEY:   '',
};
